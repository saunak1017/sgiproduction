{
  "name": "shivani-production",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/dev.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/check.mjs",
    "deploy:reminders": "npx wrangler deploy --config worker/wrangler.jsonc"
  },
  "engines": { "node": ">=22.13" },
  "devDependencies": {"jsdom":"30.1.0"}
}
